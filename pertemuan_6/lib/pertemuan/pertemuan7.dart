import 'package:flutter/material.dart';

class RadiobuttonPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Pertemuan 7"),
      ),
      body: Center(
        child: Text("Halaman Radio Button"),
      ),
    );
  }
}