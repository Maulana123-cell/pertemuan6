import 'package:flutter/material.dart';

class ListviewPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Pertemuan 5"),
      ),
      body: Center(
        child: Text("Halaman Pertemuan 5"),
      ),
    );
  }
}